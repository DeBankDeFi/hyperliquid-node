# Configuration generated by [ansible-playbook](https://github.com/DeBankDeFi/playbook). DO NOT EDIT.
# Last modified at 2023-10-23, 09:05:58.

# service_name
service_name: blockchain-hyper

# chain_id
chain_id: hyper

# rpc_listen listen address of JSON-RPC API.
rpc_listen: :9545

# forward_proxy jrpcx serve as a forward proxy.
forward_proxy: false

# internal_api_listen listen address of internal HTTP API.
# Include metrics API.
internal_api_listen: :9268

# block_interval the average time interval between block finalise by the native node,
# this configuration defines the communication interval between jrpcx and the native node.
block_interval: 10

# enable_pprof toggle go pprof.
enable_pprof: true

# native_node_url Web3 JSON-RPC endpoint of native node.
#native_node_url: https://rpc.hyperliquid.xyz/evm
native_node_url: https://mixjj7dhaf.execute-api.ap-northeast-1.amazonaws.com/hyperliquid

# official_node_url Web3 JSON-RPC endpoint of official node.
official_node_url: https://rpc.hyperliquid.xyz/evm

# observability configurations.
observability:
  static_resource:
    chain_id: hyper
    node: hybrid-a1
    group: state
  log:
    enable: true
    endpoint: fluent-bit-tcp://prod-k8-jrpcx-fluent-bit-37e181d705609ecd.elb.ap-northeast-1.amazonaws.com:5170

# processor internal processors of jrpcx that handles pre-request or(and) post-response logic.
processor:
  request_mirror:
    enable: false
  raft:
    enable: false
  rate_limiter:
    rpc_methods:
      eth_call: 1000
  observability_log:
    enable: true
    slow_threshold:
      default: 2000
      rpc_methods: {'eth_call': 1000}
  rpc_cache:
    enable: true
  multicall:
    enable: true
    gas_threshold: 0
  method_name_checker:
    enable: false
    regexp:
    denied_regexp:
  block_range_query_limit:
    enable: true
    recent_blocks: 128
  method_denied: [ ]